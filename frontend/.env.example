# Base URL of the SecondPlate API.
# Local dev: leave EMPTY — Vite proxies /api and /health to http://localhost:8000.
# Production (Vercel): set to your deployed backend, e.g. https://secondplate-api.onrender.com
VITE_API_BASE=
